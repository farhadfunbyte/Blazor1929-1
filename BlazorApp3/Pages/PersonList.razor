@* @HeaderContent *@

@*
<input class="btn btn-outline-primary" type="button" value="AddItem" @onclick="AddItem" />
 *@

<!--
    <b>ali</b>
-->

<hr />

<strong>@FirstParameter</strong>

<br/>

<strong>@FirstParameter2</strong>

<br />

@if (People != null)
{
    @foreach (var person in People)
    {
        <input class="btn btn-danger" type="button" value="Del" @onclick="@( ()=> Delete.InvokeAsync(person))" />
        <PersonDetails Person="@person" />
    }
}
else
{
    <Loading />
}

@* @FooterContent *@

@code {

    [CascadingParameter(Name = "FirstParameter")]
    private string FirstParameter { get; set; }

    [CascadingParameter(Name = "FirstParameter2")]
    private string FirstParameter2 { get; set; }


    [Parameter] public List<Person> People { get; set; }
    [Parameter] public EventCallback<Person> Delete { get; set; }
    // [Parameter] public RenderFragment HeaderContent { get; set; }
    // [Parameter] public RenderFragment FooterContent { get; set; }
}
